✦ এই প্রজেক্টটির আর্কিটেকচারটি অত্যন্ত আধুনিক এবং মডুলার। নিচে আমি পুরো প্রজেক্টটি কিভাবে কাজ করছে তা ৫টি মূল অংশে বুঝিয়ে দিচ্ছি:

  ১. অথেন্টিকেশন ও রোল ম্যানেজমেন্ট (src/auth.ts, src/app/login/)
   * NextAuth (Auth.js): এটি পুরো অ্যাপের গেটওয়ে। গুগল লগইন হ্যান্ডেল করে।
   * Role Management: ইউজার লগইন করার সময় আমরা signIn এর মাধ্যমে রিডাইরেক্ট করে দিচ্ছি। অ্যাপের ভেতর ইউজার যখন কোনো পেইজে যায়, src/app/.../page.tsx এর শুরুতে auth() কল করে চেক ক
রা                                                                                                                                                                                া হয় ইউজার কে (Customer নাকি
     Agent)।
   * Auto-upgrade: এজেন্টের ক্ষেত্রে, যদি কেউ এজেন্ট হিসেবে লগইন করে কিন্তু ডাটাবেসে রোল AGENT সেট করা না থাকে, তবে অটোমেটিক আপডেট হয়ে যাচ্ছে।

  ২. ডাটাবেস ও ডাটা মডেল (prisma/schema.prisma)
   * Prisma ORM: আমরা MariaDB ব্যবহার করছি।
   * সম্পর্ক:
       * User: সবার প্রোফাইল।
       * Conversation: একটি কাস্টমার এবং এজেন্টের মধ্যে চ্যাটের এনটিটি।
  ৩. রিয়েল-টাইম চ্যাটিং (SSE + Pub/Sub)
  এটিই এই প্রজেক্টের প্রাণ। এর তিনটি অংশ:
   * src/lib/events.ts (The Bridge): এটি একটি EventEmitter যা মেমরিতে থাকে। এটি অ্যাপের এক জায়গা থেকে অন্য জায়গায় খবর দেয়।
   * POST /api/messages (The Sender): মেসেজ পাঠানো হলে ডাটাবেসে সেভ হয়, এরপর publish() কল করে।
   * GET /.../stream (The Listener): ক্লায়েন্ট যখন SSE এর মাধ্যমে কানেক্ট করে, তখন এই রাউটটি subscribe() করে রাখে। ডাটাবেসে নতুন মেসেজ আসলে publish() এর মাধ্যমে সিগন্যাল পায় এবংস্ট্রিম হয়ে ব্রাউজারে চলে যায়।

  ৪. ক্লায়েন্ট সাইড লজিক (src/components/ChatWindow.tsx)                                                                                                                          
  ৪. ক্লায়েন্ট সাইড লজিক (src/components/ChatWindow.tsx)                                                                                                                          
  ৪. ক্লায়েন্ট সাইড লজিক (src/components/ChatWindow.tsx)
   * এটি একটি Client Component (যেহেতু use client লেখা আছে)।
   * useEffect: এটি ব্রাউজারে একটি EventSource তৈরি করে। এটি সার্ভারের সাথে একটি নিরবিচ্ছিন্ন সংযোগ বজায় রাখে।
   * setMessages: নতুন কোনো মেসেজ ইভেন্ট আসার সাথে সাথে এটি React এর স্টেট আপডেট করে, যার ফলে পেইজ রিফ্রেশ ছাড়াই মেসেজ বক্সে নতুন মেসেজ প্রদর্শিত হয়।

  ৫. প্রজেক্ট ফোল্ডার স্ট্রাকচার (workflow)                                                                                                                                        
  ৫. প্রজেক্ট ফোল্ডার স্ট্রাকচার (workflow)
   * src/app/: রাউটিং। [id] ফোল্ডারগুলো ডাইনামিক রাউট (যেমন: /agent/cmozm8...)।
   * src/api/: ব্যাকএন্ড এপিআই। ডাটা সেভ এবং স্ট্রিমিং এর কাজ এখানে।
   * server/ (যদি প্রয়োজন হয়): বর্তমানে প্রয়োজন হচ্ছে না, তবে বড় স্কেলে গেলে এখানে আলাদা সকেট সার্ভার রাখা যেত।

  ---

  আপনার কাজের জন্য workflow (প্রজেক্টে যা কিছু করবেন):
  ১. নতুন কিছু যোগ করতে চাইলে: প্রথমে prisma/schema.prisma তে ডাটা মডেল আপডেট করুন, তারপর pnpm db:migrate দিন।
  ২. নতুন পেইজ করতে চাইলে: src/app/ এর ভেতর ফোল্ডার তৈরি করুন এবং auth() চেক দিয়ে রোল নিশ্চিত করুন।
  ৩. মেসেজ সিস্টেম এডিট করতে চাইলে: src/api/messages/route.ts এবং src/components/ChatWindow.tsx এই দুটি ফাইল সবচেয়ে গুরুত্বপূর্ণ।
